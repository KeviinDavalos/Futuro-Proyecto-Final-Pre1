/*
Intenten llevar un orden en el código, es decir, primero definan la clase que van 
a usar, luego el array donde van a almacenar los objetos, luego los objetos y 
finalmente las funciones y las llamadas a las mismas. La idea es que la aplicación 
sea interactiva, es decir que el usuario ingrese datos, esos datos sean procesados 
mediante funciones, dicho de otra forma hacer algo con los datos ingresados, y luego
mostrar alguna salida o resultado por alert() o consola.
*/

let producto